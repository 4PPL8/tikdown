{
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "cd backend && pip install yt-dlp && npm install"
  },
  "deploy": {
    "startCommand": "cd backend && npm start",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}